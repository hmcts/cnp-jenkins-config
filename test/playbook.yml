---
- hosts: docker-role-test
  pre_tasks:
    - name: Ensure jenkins group is present
      group:
        name: jenkins
        state: present
    - name: Ensure jenkins user is present
      user:
        name: jenkins
        group: jenkins
  roles:
    - role: roles/docker
